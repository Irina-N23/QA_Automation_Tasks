package automation.cleancode.planes;

public class NewPlanePrototype extends Plane {
    public NewPlanePrototype(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
            super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}